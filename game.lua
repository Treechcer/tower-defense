game = {
    width = love.graphics.getWidth(),
    height = love.graphics.getHeight(),
    world = "world1",
    level = "level1",
    defaults = {
        startMoney = 500
    }
}

game.money = game.defaults.startMoney

return game